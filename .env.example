# Example .env for Raseed
PROJECT_ID=your-gcp-project-id
REGION=us-central1
GOOGLE_WALLET_ISSUER_ID=issuer-id-from-google

# Service Account Authentication (replaces FIREBASE_TOKEN)
# For local development and CI/CD
GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account.json

# Infrastructure outputs
RECEIPT_BUCKET=your-gcp-project-id-receipts
FUNCTIONS_SA=cloud-functions-sa@your-gcp-project-id.iam.gserviceaccount.com

# Note: FIREBASE_TOKEN is deprecated and no longer needed
# The Firebase CLI now uses Application Default Credentials (ADC)
